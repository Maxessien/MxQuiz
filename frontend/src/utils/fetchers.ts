export const getQuiz = (
  sortBy: "ratings" | "date" | "attempts",
  order: "asc" | "desc",
  search: string,
  type: ("mcq" | "theory")[],
) => {
    try {
        
    } catch (err) {
        
    }
};
